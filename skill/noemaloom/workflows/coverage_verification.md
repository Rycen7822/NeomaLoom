# coverage_verification

After edits, run coverage verification against changed paths, old terms, new terms, anchors, links, and expected role coverage. Treat missing coverage as an implementation blocker.
